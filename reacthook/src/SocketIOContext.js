import React from 'react';

// JMS: This is almost like defining a type, even though it looks like we are just creating an instance
// Have a look at the documentation for React context, but it's a no brainer for your socket IO instance.
export const SocketIOContext = React.createContext();